import Foundation

struct DataBridge {
    // TODO: Path to conductor.db (resolve from .conductor/conductor.db)
    // TODO: func fetchProjects() -> [ProjectStatus] — shell out to sqlite3
    // TODO: func fetchSessions() -> [AgentSession] — shell out to sqlite3
    // TODO: func fetchFeatureCounts(project: String) -> (passed: Int, total: Int)
    // TODO: Private helper to run sqlite3 via Process() with JSON output mode
}
